# Echo Skill

Reference plugin package used by Rove tests and docs.

This package is intentionally small:

- one tool
- one runtime catalog
- placeholder signatures
- dummy wasm payload

Use it when validating:

- `rove plugin pack`
- `rove plugin publish`
- registry layout
- documentation examples
